
说明：
    1.把数据直接扔到数据库中，不关心sql语句。
    2.从数据库取数据，也是直接通过model的类名取。不用写sql

注意：
    1.不能将sql语句的关键字当作属性名（index, select, insert,into,table 等），否则执行sql会报错，导致sql语句执行失败。

思路：
    1. 对象是组织存储数据的一种形式，数据库也是。这个小框架就是在两端做一个映射。
    2. 在MJExtention的1.0版本上改动实现的。

局限性：
    1.无法处理，复杂的model。如Persion类中持有Book型类的实体或者Persion类中持有数组，数组中放的Book类型的实体。原因是复杂的实体类需要做表关联，而FMDB不好做。
    2.model的属性的数据类型只能是NSString或者NSdata类型。

